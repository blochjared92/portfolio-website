/* ============================================================
    DESIGN: Theme toggle disabled - dark mode only
    ============================================================ */
export default function ThemeToggle() {
  return null;
}
